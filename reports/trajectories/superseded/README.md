# Superseded trajectories

These two were read from the localhost benchmark of 2026-09-18. They are kept
because they were genuinely read and because deleting evidence to tidy a
submission is not something this project does.

They were replaced, not corrected: the authoritative benchmark now runs against
the deployed application, and a trajectory a grader reads should come from the
population that was actually evaluated. The runs behind them live in
`runs/failing/` (the failing one, which errored) and in the `lumina` database.
