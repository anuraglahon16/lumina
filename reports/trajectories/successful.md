req_mu7u5dw4wvyjjn — "Why do proxies buffer Server-Sent Events?" Quick, web mode, on the deployed application: 3271ms end to end, TTFT 614ms, $0.005296, terminated sufficient_evidence. The same question as the trajectory I read on localhost, which is why I chose it: it is the one run I can compare across the two environments, and the comparison is the point. Context assembly took 5ms and found no memories for this user. The search came back in 0ms with six results — a cache hit, which is worth noticing because the deployed cache hit rate is 44.7% and below its target, so this run is the good case rather than the typical one. Then the fetch pool works the six candidates concurrently and two of the four attempts are refused: Medium 403s in 25ms, Server Fault 403s in 69ms. Two succeed — a OneUptime post on nginx and SSE giving 5591 characters in 3ms from the page cache, and a Cypress GitHub issue giving 1582 characters in 73ms. Retrieval was 74ms of the 3271ms. Synthesis was 3172ms, which is essentially the whole run, and memory extraction added 1669ms after the answer had already been sent to the reader. Five sentences cited, five supported, groundedness 1.0, two sources cited of six discovered. What it taught me. First, the shape of the run is now almost entirely synthesis: retrieval is 2% of the wall clock and the model writing the answer is 97%. Every instinct to optimise retrieval further is misdirected effort — the TTFT work that matters is in the first token, not in the fetches. Second, a 50% fetch refusal rate is the ordinary case on the open web and the concurrent pool is what survives it; sequentially this run would have spent two round trips on 403s before reading anything. Third, and this is the one I got wrong earlier: on localhost this same question produced an internal groundedness of 1.0 and still failed the benchmark's provenance check, because our extracted snippet was not a contiguous twelve-token window of the live page. Deployed, with the locator and extraction fixes in place, the benchmark scored 75 of 75 citations as verifiable. Internal grounding and graded provenance are different questions, and the second one only became true when I stopped treating the first as evidence for it.
