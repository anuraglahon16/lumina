req_mu7upct3qus33l — "What are the trade-offs between MongoDB Atlas Vector Search and a dedicated vector database for a RAG application with page-level citations?" Deep, on the deployed application: 42536ms end to end, $0.103941, terminated deep_tool_budget_exhausted. This is the run that exposed the budget defect, and it is worth reading precisely because it produced a good answer while doing something the design did not intend. The plan took 2211ms and returned four sub-questions — citation preservation and metadata, performance and scaling, infrastructure cost at scale, and current limitations. Research ran 16136ms across four concurrent branches, each allotted six tool calls. Every branch used five, each had one call refunded after a 403, and all four ended capped: three against their own per-branch ceiling and the fourth, q4, against the shared pool. The sweep then ran for 1019ms, synthesis for 23151ms, and memory extraction for 1358ms after the answer was sent. It discovered 60 candidates, fetched 11, cited 10, and wrote 6005 characters at a groundedness of 0.895. Here is the defect. The run log records 31 tool calls — 12 searches and 19 fetches — against a global ceiling of 24. The pool itself only ever saw 22 claimed, because the cross-branch sweep did not claim slots at all: it called the fetcher directly, so its pages were real provider calls that no budget counted. Meanwhile maxToolCallsPerBranch of 6 times four branches is exactly maxToolCallsTotal of 24, so the branches were guaranteed to consume the entire pool and leave the sweep nothing, which is why q4's next call was refused by a budget that four branches had already spent. Three things follow. First, a ceiling that a later phase can step around is not a ceiling, and the only reason this looked fine for so long is that the grader counts trace events and the sweep does not emit them — the visible number stayed under 24 while the real one did not. Second, the arithmetic was self-defeating rather than merely tight: no amount of tuning the branch allowance fixes a design where the branches are entitled to the whole pool by construction. Third, and the reason this run is the failing trajectory rather than a footnote, it terminated honestly. It said deep_tool_budget_exhausted, it kept its partial answer, and it cited ten sources — the run told the truth about being curtailed, which is what let me find the defect at all. The fix was to allocate before the branches start, reserve capacity for the sweep, make the sweep claim from the same pool, and let a branch that spends its allocation finish normally rather than by being refused.
